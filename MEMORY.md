# Memory

Past.ioは既存の複雑なPVP実装を残すより、ユーザー要求の最小スコープに合わせてサーバーとクライアントの入口を整理した。ゲームはSocket.IOによるルーム同期を前提とし、ルーム作成時にseedを確定する。フックはinput.hookを60fps相当で送信し、サーバー側で足場とプレイヤーを同じターゲット選択系に通している。pointerup、pointercancel、window blurでクライアントの長押し状態を解除する。

マップは18段の足場と拾い物、フィニッシュ地点だけを生成する。落下時は開始足場へ戻す。拾い物は共有マップ上で最初に接触したプレイヤーへ加点し、全クライアントへイベントを送る。

生成素材はホーム背景、ロゴ、ルーム背景テクスチャの3点に抑え、足場やプレイヤーはCanvas描画でクレジットとデプロイ負荷を削減した。


## Browser verification

ローカルの3300番でホームを開き、名前入力後にルーム作成を実行できた。ルーム画面では4文字コード、seed、プレイヤー数、HOST表示、START/LEAVE操作が確認できた。ホーム背景の生成ビジュアルとロゴURLは表示され、フォームの文字コントラストも確保されている。

プレイ画面では、seedから生成された複数段の足場、拾い物、ゴール旗、プレイヤー、四隅HUDが描画された。Canvasの入力対象とEXIT操作が表示され、A/D・SPACE・CLICKの操作表示も画面下に収まっている。


## Update verification

更新後の3300番ローカル画面では、生成ロゴとPAST.ioブランドが表示され、ホームからジャンプ操作の導線はなく、ゲーム内操作はA/DとCLICK HOLDだけになる構成を確認した。


更新後のプレイ画面では、プレイヤーがパスタ形状になり、HUDが `LEVEL 07` と `A / D`、`CLICK HOLD` を表示した。従来のSpace操作は画面から消えている。Canvasへの長押しpointerdown/pointerup入力も送信できた。


## Redesign verification

ローカル3300番で再設計後のホーム画面を確認した。既存の生成ロゴとPast.ioの工業スポーツUIは維持され、ゲーム画面の操作表記はA/DとCLICK HOLDに限定されている。


再設計後のブラウザ確認では、seed・チャンク由来の複数足場・移動足場マーカー・パスタ本体・A/D・CLICK HOLD HUDを確認できた。低層では複雑なギミックがまだ画面上部に隠れるため、検証はSocket.IOのマップデータと合わせて行う必要がある。


手作りチャンク版のホーム画面は再表示でき、検証名CHUNKPASTAを入力できた。前回同様、ホームの生成ロゴと既存のPast.io UIを維持している。


手作りチャンク版でルーム作成が成功し、4文字ルームコード、seed、CHUNKPASTAのプレイヤー表示、START導線を確認した。


手作りチャンク版の実機画面では、開始直後から横位置の異なる複数足場、移動足場の青緑マーカー、拾い物、パスタ本体、A/D・CLICK HOLD HUDが確認できた。Socket.IOスモークテストでは8チャンク、複数ギミック、プレイヤー対象麺フック、離上解除を確認済み。


## Attachment spec verification

添付画像をWebアセットとして登録し、ローカル限定ルートでもPNG配信を確認した。ホーム文言はNOODLE ARENAへ変更され、ルーム作成も成功した。現在はパスタ画像をプレイヤーCanvasへ描画し、横2Dアリーナの地形・固定面・可動物体を同期する構成になっている。


ブラウザのゲーム画面で添付画像由来の黄色いパスタ、赤いソース、黒いローラー足、黄色いフォーク、後方のフォークがプレイヤーとして表示された。横2Dアリーナには太い壁・床・天井、複数の狭い通路、固定面、箱型の可動物体が同時に見える。Canvasへの可動物体方向の長押し入力も送信した。


## Noodle-only input verification

ブラウザの更新後ホーム／ルーム画面を確認し、ゲーム起動フローは維持されている。A/D操作をクライアント、サーバー、README、HUDから削除し、マウス照準と左クリック長押し／解除のみを操作表示にした。Canvas上の固定物は壁・床・天井・梁の矩形面からフック候補を生成する。

## Vector party-game redesign

2026-09-01: 既存の工業スポーツ表現を、白い皿の余白・アルデンテイエロー・トマトレッド・チャコールの4色によるベクター・パスタ・ウェスタンへ変更した。Canvas描画は円・線・矩形・多角形だけで構成し、外部画像素材への依存をなくした。

ゲームモードは `tag`（鬼ごっこ）、`hide`（隠れんぼ）、`free`（フリーモード）、`hill`（ヒルアンドハイ）、`pvp`（ソース決闘）をルーム作成時に選択できる。ワールドは 18000 x 9800 の大規模空間として、床・天井・外壁・100個以上の固定足場・18個のランドマークをseedから生成する。

PvPではハンマー（重量4.8、heavy-hit）、銃（重量0.8、recoil-shot）、石ころ（重量1.2、bonk）を動的オブジェクトとして実装した。PvP道具もフック対象として麺の張力で動かし、衝突速度と重量に基づくダメージとノックバックを同期する。オブジェクトには `weight` と `effect` を持たせる。

`node --check server/server.js` と `node --check docs/game.js` は成功。ローカルサーバーを一時起動し、`/` が `PASU.io` と Canvasを返すことを確認した。外部画像参照はクライアントから除去済み。

## Two-stage hook restoration

2026-09-01: フック／ハンドを唯一の基本アクションとして再整理。`pointerdown` で照準ターゲットへ麺を一度発射して接続し、接続中は押下を維持した `pointermove` で張力と方向を操作、`pointerup` で解除する2段階操作に戻した。`grab`、`use`、Spaceなどの別入力は削除した。

フック対象は、壁・床・天井・固定足場など全ての不動態矩形、箱・PvP道具の動態オブジェクト、他プレイヤー。サーバーは接続後に対象を再選択せず、不動態ではプレイヤー、動態では対象物、プレイヤー対象では双方へ元仕様の張力・接線方向の反作用を適用する。

スマホの長押し誤コピー対策として、ゲームCanvasへ `touch-action: none`、`user-select: none`、`-webkit-touch-callout: none` を付け、touchstart・touchmove・touchend・touchcancel・contextmenu・selectstart・dragstartを抑止した。フォーム入力には制限を掛けていない。

## Physical surface hook

2026-09-01: 仮想的なアンカー点を完全に使わない仕様へ更新した。クリック位置までの線分を作り、壁・床・天井・固定足場・動態オブジェクト・他プレイヤーの実矩形との交差を `lineRectHit` で判定する。線分の始点から最も近い交差だけを接続対象にするため、マップ上のどこでも実物へ引っ掛けられる。

フック最大距離は760ワールド単位、フック最大力は2.6。接続後に対象距離が上限を超える場合は解除し、計算後の合力も最大力でクランプする。不動態ではプレイヤー、動態では対象物、プレイヤー対象では双方へ力を適用する。

## Mode-specific terrain and online spawn

2026-09-01: 各モードに `MODE_LAYOUTS`、`MODE_SPAWNS`、`MODE_OBJECTS` を導入した。TAGは周回とゲート、HIDEは天井付き部屋と縦壁、FREEは高低差とゲート、HILLは階段状の登坂、PVPは長い闘技場と仕切りを持つ。共通の外壁・床・天井に加えて、段差、縦穴、分岐、狭い通路、囲いを配置し、seed由来の散在足場も加える。

新規参加とリスポーンは `spawnPointFor` を通り、既存プレイヤーから180ワールド単位以上離れたモード専用地点を選ぶ。地点が埋まった場合は安全グリッドへフォールバックする。クライアント右上の `miniMap` Canvasには固定地形、道具、プレイヤー、現在の視界枠を描画する。

## Hyper-noodle physics tempo

2026-09-01: 全体テンポを上げるため物理更新周期を50msから約33msへ短縮。プレイヤー最大速度18、重力0.68、動態オブジェクト重力0.62、動態オブジェクト速度上限28へ調整した。麺の張力係数は0.08、フック最大力は12、接線方向の操作力は0.95へ強化し、重量4.8のハンマーでもフックで振り回せるようにした。動態オブジェクトの水平減衰は0.975へ緩和して勢いを残す。

## Ceiling, toned physics, and round rules

2026-09-01: プレイヤーと動態オブジェクトに天井Y=40の上端境界を追加し、上へ抜けた場合は位置を境界へ戻して上向き速度を0にする。衝突解決も床だけでなく上面・下面・左右面へ拡張し、縦壁・天井・複雑な囲いを機能させた。

動態オブジェクトへのフック力は以前の逆符号を修正して`vy`へ同方向に適用し、上にあるオブジェクトを正しく持ち上げられるようにした。強すぎた挙動はフック最大力8、動態操作係数2.2、張力係数0.055へ下げた。

`MODE_RULES` と `updateRound` を追加した。TAGは5タッチ、HIDEは隠れ30秒、FREEは20ポイント、HILLは頂上滞在30秒、PVPは最後の1人で勝利。モードごとの制限時間と時間切れ時の比較値を状態配信し、ルーム画面・HUDへルールと勝利条件を表示する。

## Rules, ceiling safety, and terrain redesign

2026-09-01: 天井Y=40をプレイヤーと動態オブジェクトの上端境界として追加した。物理衝突を床だけでなく上面・下面・左右面へ拡張し、天井抜け、縦壁すり抜け、複雑な囲いからの不正脱出を抑止する。動態オブジェクトの上方向フック力は`targetObject.vy += forceY * objectControlForce / weight`へ修正した。

麺が強すぎたため、フック最大力8、張力係数0.055、動態操作係数2.2、プレイヤー速度17、重力0.6、動態重力0.54へ再調整した。道具は振り回せるが、以前の最大力12より制御しやすい。

各モードに勝利条件と制限時間を追加した。TAGは5タッチ／90秒、HIDEは隠れ30秒／90秒、FREEは20ポイント／120秒、HILLは頂上滞在30秒／120秒、PVPは最後の1人／180秒。`room.ended`、`winnerId`、`endReason`、`roundEnd`で終了を同期し、ルーム画面とHUDへルールと勝利条件を表示する。

地形はモードごとのレイアウトとランドマークへ整理し、TAGは周回、HIDEは部屋型迷路、FREEは自由探索、HILLは登坂、PVPは仕切り付き闘技場として構造的に差別化した。クライアント描画も水平足場、縦壁、ゲート、天井をタグ別に色分けする。
