# Game Plan: Past.io Party

## Risk Tasks

### 1. 手続き生成の縦型マップ
- **Why isolated:** 毎回異なる足場配置でも、開始地点から上方向へ進めるルートと画面内の視認性を保つ必要がある。
- **Approach:** ルーム作成時にseedを確定し、段ごとに足場・拾い物・ゴールを生成する。クライアントはサーバーから受け取った同一のマップだけを描画する。
- **Verify:** 同じseedのルームでは全プレイヤーに同じ足場が表示され、開始地点からゴールまで到達可能な足場が続く。

### 2. 長押し継続フック
- **Why isolated:** 押下開始、押下継続、離上解除を分け、足場と他プレイヤーの両方を対象にしても入力の取りこぼしを防ぐ必要がある。
- **Approach:** クライアントはpointerdown中だけhook=trueを毎フレーム送信し、サーバーは最初のフレームで対象を選び、継続中は距離制限内で牽引する。pointerup・blur・disconnectで必ず解除する。
- **Verify:** 足場またはプレイヤーを長押し中だけラインが存在し、離すと即時解除される。対象プレイヤーも引き寄せられ、対象が離れた場合は安全に解除される。

## Main Build

ホーム画面、ルーム作成・参加、ルーム待機、Canvasゲーム画面を一つの小さなクライアントにまとめる。サーバーはSocket.IOでルーム、プレイヤー状態、seed付き自動生成マップ、拾い物、フック状態を同期する。拾い物は接触で取得し、ルーム内の共有スコアに反映する。

- **Assets needed:** 世界観確認用スクリーンショット、ロゴ記号、工業素材テクスチャ。ゲーム内の足場・プレイヤー・拾い物はCanvas描画で生成する。
- **Verify:**
  - A/Dまたは矢印キーで移動、Spaceでジャンプ、マウス長押しでフックが継続する。
  - ルームコードの作成・参加・退出が動作する。
  - 複数クライアントでプレイヤー位置、フック、拾い物取得が同期する。
  - UIに余計な説明文やプレースホルダーがなく、ゲーム状態が画面端で読める。
  - 生成素材が欠落せず、背景とUIのコントラストが保たれる。
  - ブラウザコンソールに実行時エラーがない。
  - reference.pngとの一貫性: 安全色、工業的な質感、縦に登る画面構成。
